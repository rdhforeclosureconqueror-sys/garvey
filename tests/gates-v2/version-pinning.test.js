'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup,experience}=require('./persistence-helpers');test('offering changes never rewrite historical pins; withdrawal fails closed',()=>{const p=setup(),x=experience();p.releases.createRelease({release_id:'release-2',release_version:'2',manifest_hash:'hash-2',manifest_json:{experiences:[x]},created_at:'2026-02-01'});p.releases.setStatus('release-2','published','2026-02-01');assert.equal(p.releases.selectOfferedRelease().release_id,'release-2');p.releases.setStatus('release-1','retired','2026-02-02');assert.equal(p.sessions.resumeSession({session_id:'session-1',parent_profile_id:'parent-1',child_id:'child-1'}).release_id,'release-1');p.releases.setStatus('release-1','safety_withdrawn','2026-02-03','safety');assert.throws(()=>p.sessions.resumeSession({session_id:'session-1',parent_profile_id:'parent-1',child_id:'child-1'}),e=>e.code==='CONTENT_RELEASE_SAFETY_WITHDRAWN');assert.equal(p.sessions.getSessionById('session-1').release_id,'release-1');});
