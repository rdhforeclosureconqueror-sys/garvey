'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup,apply}=require('./persistence-helpers');test('identical retry is stable and not duplicated',()=>{const p=setup(),a=apply(p),b=apply(p);assert.deepEqual(b,a);assert.equal(p.sessions.getSessionById('session-1').revision,1);assert.equal(p.events.listSessionEvents('session-1').length,2);});test('changed payload conflicts',()=>{const p=setup();apply(p);assert.throws(()=>apply(p,{action:{type:'VIEW_NODE'}}),e=>e.code==='IDEMPOTENCY_CONFLICT');});
