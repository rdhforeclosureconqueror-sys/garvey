'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
test('transaction coordinator uses one client, row lock, CAS, and transaction controls',()=>{const dir=path.join(__dirname,'../../gates-v2/persistence/postgres');const tx=fs.readFileSync(path.join(dir,'postgresTransactionalExperienceRepository.js'),'utf8'),sessions=fs.readFileSync(path.join(dir,'postgresSessionRepository.js'),'utf8'),queries=fs.readFileSync(path.join(dir,'postgresQueries.js'),'utf8');assert.match(tx,/pool\.connect\(\)/);assert.match(tx,/BEGIN/);assert.match(tx,/COMMIT/);assert.match(tx,/ROLLBACK/);assert.match(queries,/FOR UPDATE/);assert.match(sessions,/WHERE session_id=\$1 AND revision=\$5/);});
