'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup,apply}=require('./persistence-helpers');test('session id alone cannot authorize reads or mutations',()=>{for(const [patch,code] of [[{parent_profile_id:'wrong'},'SESSION_OWNERSHIP_MISMATCH'],[{child_id:'wrong'},'CHILD_OWNERSHIP_MISMATCH']]){const p=setup();assert.throws(()=>p.sessions.getOwnedSession({session_id:'session-1',parent_profile_id:'parent-1',child_id:'child-1',...patch}),e=>e.code===code);assert.throws(()=>apply(p,{...patch,idempotency_key:'x'}),e=>e.code===code);assert.equal(p.sessions.getSessionById('session-1').revision,0);}});
