'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup}=require('./persistence-helpers');test('duplicate sequence rejected and no mutation methods exist',()=>{const p=setup(),e={event_id:'e',session_id:'session-1',sequence:1,event_type:'x',idempotency_key:'k',event_json:{type:'x'},occurred_at:'t',received_at:'t'};p.events.append([e]);assert.throws(()=>p.events.append([{...e,event_id:'e2',idempotency_key:'k2'}]),x=>x.code==='EVENT_SEQUENCE_CONFLICT');assert.equal(p.events.update,undefined);});
