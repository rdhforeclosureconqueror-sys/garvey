'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');test('SQL contracts create only separate V2 tables and protections',()=>{const dir=path.join(__dirname,'../../gates-v2/persistence/migrations'),files=fs.readdirSync(dir).sort(),sql=files.map(x=>fs.readFileSync(path.join(dir,x),'utf8')).join('\n');assert.equal(files.length,3);for(const t of ['content_releases','experience_sessions','experience_events'])assert.match(sql,new RegExp(`gates_v2_${t}`));assert.match(sql,/UNIQUE\(session_id,sequence\)/);assert.match(sql,/append-only/);assert.doesNotMatch(sql,/ALTER TABLE gates_(story|guidance|assessments|progress)/i);});
