'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup,apply}=require('./persistence-helpers');test('event failure rolls back staged writes',()=>{const p=setup();p.transactions.eventId=()=>{throw Error('injected')};assert.throws(()=>apply(p),e=>e.code==='PERSISTENCE_TRANSACTION_FAILED');assert.equal(p.sessions.getSessionById('session-1').revision,0);assert.deepEqual(p.events.listSessionEvents('session-1'),[]);});
