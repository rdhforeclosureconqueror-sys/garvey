'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup,apply}=require('./persistence-helpers');test('prior history survives later rejected action',()=>{const p=setup();apply(p);const before=p.events.listSessionEvents('session-1');assert.throws(()=>p.transactions.transactionallyApplyAction({session_id:'session-1',parent_profile_id:'parent-1',child_id:'child-1',expected_revision:1,idempotency_key:'bad',action:{type:'UNKNOWN'},occurred_at:'later'}));assert.deepEqual(p.events.listSessionEvents('session-1'),before);});
