'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {setup,apply}=require('./persistence-helpers');test('resume exact node; malformed state fails closed',()=>{const p=setup();apply(p);assert.equal(p.sessions.resumeSession({session_id:'session-1',parent_profile_id:'parent-1',child_id:'child-1'}).state_json.current_node_id,p.x.entry_node_id);p.store.sessions.get('session-1').state_json='{"bad":true}';assert.throws(()=>p.sessions.getSessionById('session-1'),e=>e.code==='INVALID_STORED_SESSION_STATE');});
