'use strict';const test=require('node:test'),assert=require('node:assert/strict'),v=require('../../gates-v2/validation/validateContentManifest'),{load,clone}=require('./helpers');const good=()=>load('content-manifest.example.json');test('draft manifest validates but is blocked from publication',()=>{const r=v(good());assert.equal(r.valid,true);assert.ok(r.publicationBlockers.length)});test('unsupported schema version fails',()=>{const x=clone(good());x.schema_version='v9';assert.equal(v(x).valid,false)});test('duplicate content entry fails',()=>{const x=clone(good());x.definitions.push(clone(x.definitions[0]));assert.equal(v(x).valid,false)});
